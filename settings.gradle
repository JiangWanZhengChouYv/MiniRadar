pluginManagement {
    repositories {
        maven { url "https://maven.neoforged.net/releases" }
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven { url "https://maven.neoforged.net/releases" }
        mavenCentral()
    }
}

rootProject.name = "MiniRadar"