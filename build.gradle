plugins {
    id 'java-library'
    id 'net.neoforged.gradle.userdev' version '7.0.142'
}

// 就留这一行，别的全删！干净到极致！
java {
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
}

// 官方必写，解决类找不到
neoForge {
    version = "1.20.1-47.1.0" // 直接写死版本，不报变量错
}

// 极简资源处理
processResources {
}